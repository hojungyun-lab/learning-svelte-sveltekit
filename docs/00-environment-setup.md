# 🛠️ Step 00: 환경 설정

## 학습 목표
- Node.js 설치 확인
- VS Code Svelte 확장 설치
- 학습용 SvelteKit 프로젝트 생성

---

## 1. Node.js 확인 및 버전 관리 (NVM)

웹 개발에서는 프로젝트마다 요구하는 Node.js 버전이 다를 수 있습니다. 파이썬의 `pyenv`처럼 프로젝트별로 버전을 완벽하게 격리하고 쉽게 교체할 수 있게 해주는 가장 표준적인 도구가 바로 **NVM (Node Version Manager)** 입니다.

### 1-1. NVM 설치 (macOS/Linux 기준)
```bash
# NVM 설치 스크립트 다운로드 및 실행
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
```
*(설치 후 터미널을 완전히 껐다 켜야 `nvm` 명령어가 인식됩니다.)*

### 1-2. Node.js (v22 LTS) 설치
NVM이 설치되었다면, SvelteKit 구동에 필요한 최신 안정화 버전(LTS)을 설치하고 기본값으로 설정합니다.

```bash
nvm install 22       # Node.js 22 버전 설치
nvm use 22           # 현재 터미널에서 22 버전 사용
nvm alias default 22 # 터미널을 새로 열 때마다 기본으로 22 버전 사용설정
```

### 1-3. 최종 확인
```bash
node --version   # v22.x.x가 출력되는지 확인
npm --version    # 10.x.x (Node를 설치하면 npm도 함께 설치됨)
```

---

## 2. VS Code 확장

```bash
code --install-extension svelte.svelte-vscode
```

---

## 3. 학습용 프로젝트 생성

학습하며 직접 완성해나갈 앱을 생성합니다. (참고: `examples/` 디렉토리는 완성된 예제 코드 모음입니다.)

```bash
# 프로젝트 루트(learning-svelte-sveltekit)에서 실행
# 학습용 SvelteKit 프로젝트 생성
npx -y sv create my-app --template minimal --types ts

# ❗️ 만약 "What would you like to add to your project?" 질문이 나오면,
# 아무것도 선택하지 말고(키보드 화살표 ✘) 바로 Enter를 누르세요.

# 의존성 설치
cd my-app
npm install
# 테스트 환경 구축용 패키지 추가
# - vitest: Vite 기반의 차세대 테스트 프레임워크 (빠른 실행 속도)
# - @testing-library/svelte: Svelte 컴포넌트 렌더링 및 DOM 조작/검증 유틸리티
# - jsdom: Node.js 환경에서 브라우저 DOM API를 에뮬레이션하여 UI 테스트 지원
npm install -D vitest @testing-library/svelte jsdom

# 개발 서버 시작
npm run dev -- --open
```

`http://localhost:5173`에서 Welcome 페이지가 표시되면 성공! 🎉

---

## 4. 프로젝트 구조

생성된 `my-app/` 구조는 다음과 같습니다:

```
my-app/
├── src/
│   ├── routes/          # 페이지 (파일 기반 라우팅)
│   │   └── +page.svelte # 홈페이지
│   ├── lib/             # 공유 라이브러리 ($lib)
│   ├── app.html         # HTML 셸
│   └── app.d.ts         # TypeScript 타입
├── static/              # 정적 파일
├── svelte.config.js     # Svelte 설정
├── vite.config.ts       # Vite + Vitest 설정
├── tsconfig.json        # TypeScript 설정
└── package.json
```

---

## ✅ 확인

- [ ] `node --version` → v22+
- [ ] `npm --version` → v10+
- [ ] VS Code Svelte 확장 설치 완료
- [ ] `npm run dev` → 개발 서버 정상 시작
- [ ] `http://localhost:5173` → 페이지 정상 표시

---

## 🎯 다음 단계

[02-svelte-basics.md](./02-svelte-basics.md) →
